# syntax=docker/dockerfile:1
FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 3000
ENV PORT=3000 HOSTNAME=0.0.0.0 NODE_ENV=development

CMD ["npm", "run", "dev"]
